# 复制为 backend/.env 并按需修改；本文件被 .gitignore 排除，请勿提交真实凭据。
# Makefile 顶部 `-include .env` + `export`，下列变量会注入到 `make` 启动的子进程。

# 迁移工具读取的 DSN（make migrate / migrate-down / migrate-status）。
DB_DSN ?= postgres://fundpilot:fundpilot@localhost:5432/fundpilot?sslmode=disable

# asset repo 集成测试（internal/asset/repo_test.go）使用的 DSN。
# 未设置时，相关测试 t.Skip——平台层测试仍正常跑。
# 注意：测试会 TRUNCATE positions / position_valuations，不要指向生产库。
FUNDPILOT_TEST_DSN ?= postgres://fundpilot:fundpilot@localhost:5432/fundpilot?sslmode=disable
