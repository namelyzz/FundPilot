# frontend/

V0.1 阶段**占位目录**，本目录暂不包含任何代码。

V0.2 启用时预期技术栈（可调整）：

- 框架：React + Vite
- 语言：TypeScript
- 状态管理：待定（Zustand / Redux Toolkit）
- 请求层：基于后端 OpenAPI 生成的 TS 客户端（`backend/api/openapi.yaml`）
- 样式：待定（Tailwind / antd / shadcn-ui）

启用前不要在这里写任何 V0.1 代码——V0.1 仅以 REST API 形式对外暴露，前端验证用 curl / Postman / Swagger UI。
